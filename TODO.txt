TODO: Implement Atomic File Replacement for all vault write operations.
- Do not change a secret's UUID when editing its value.
- To prevent file corruption during rsync/daemon reads, all writes must follow the atomic pattern: Write to `[UUID].age.tmp`, call `fsync`, then use `os.Rename` to overwrite the existing `[UUID].age`.
